public class BlackTile implements Tile{// grants it all the tile atributes and also checks the tile type so it can be implemented in the game.

    @Override
    public String getTileType() {
        return "Black";
    }
}
